import { Entity, UUID, EntityType } from '@lib';
import { createAction, props } from '@ngrx/store';

// export const createEntity = createAction('[CORE] Create Entity', props<{ entity: EntityType, data: Partial<Entity> }>());
// export const createEntitySuccess = createAction('[CORE] Create Entity Success', props<{ id: UUID, entity: EntityType }>());

// export const updateEntity = createAction('[CORE] Update Entity', props<{ id: UUID, entity: EntityType, data: Partial<Entity> }>());
// export const updateEntitySuccess = createAction('[CORE] Update Entity Success', props<{ id: UUID, entity: EntityType }>());

// export const deleteEntity = createAction('[CORE] Delete Entity', props<{ entity: EntityType, id: UUID }>());
// export const deleteEntitySuccess = createAction('[CORE] Delete Entity Success', props<{ entity: EntityType }>());
